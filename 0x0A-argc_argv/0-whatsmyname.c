#include <stdio.h>
#include <stdlib.h>

/**
 * main - the main function
 * @argc: argument count
 * @argv: argument array
 * Return: 0
 */
int main(int argc, char *argv[])
{
int __attribute__((unused)) fvoid = argc;
printf("%s\n", argv[0]);
return (0);
}
